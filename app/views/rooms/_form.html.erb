<%= form_with(model: room, local: true) do |form| %>
  <% if room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
      <% room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :room_name %>
    <%= form.text_field :room_name, id: :room_room_name %>
  </div>

   <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address, id: :room_address %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :room_description %>
  </div>

  <div class="field">
    <%= form.label :bedrooms %>
    <%= form.number_field :bedrooms, id: :room_bedrooms %>
  </div>

  <div class="field">
    <%= form.label :beds %>
    <%= form.number_field :beds, id: :room_beds %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
